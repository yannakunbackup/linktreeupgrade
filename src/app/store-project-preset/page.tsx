import StorePage from '@/components/store-page';

export const metadata = {
  title: 'Store — Project & Preset by Mathaoduoyu',
};

export default function ProjectPresetStore() {
  return (
    <StorePage
      collectionName="projectPresetProducts"
      title="STORE"
      subtitle="project & preset matha"
    />
  );
}