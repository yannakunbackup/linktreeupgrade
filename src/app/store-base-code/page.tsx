import StorePage from '@/components/store-page';

export const metadata = {
  title: 'Store — Base Code by Mathaoduoyu',
};

export default function BaseCodeStore() {
  return (
    <StorePage
      collectionName="baseCodeProducts"
      title="STORE"
      subtitle="base code matha"
    />
  );
}
